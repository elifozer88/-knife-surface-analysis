# Knife Surface Analysis

Project scaffold for knife surface analysis.

Folders:
- `data/raw` — original files
- `data/processed` — cleaned data
- `notebooks` — EDA notebooks
- `src` — scripts and models
- `outputs` — figures and trained models
